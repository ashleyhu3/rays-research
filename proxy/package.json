{
  "name": "options-proxy",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Serverless Yahoo Finance options-chain fetcher for the signal-dashboard (bypasses Render's rate-limited egress IPs).",
  "dependencies": {
    "yahoo-finance2": "^3.15.2"
  }
}
